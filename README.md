The function `area_of_circle(...)` in the `main.rs` file returns the area of a circle given the input argument `radius`. The function is unitless. 
   
Copilot is interesting to me because it can explain or answer question as an instructor would do. 

